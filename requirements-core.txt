# Minimal direct dependencies for running the tutorial.
# Loosely pinned so they resolve cleanly against Colab's preinstalled packages
# (the fully-pinned `requirements.txt` is a frozen env meant for a local venv).
ppi-python>=0.2.3
openai>=1.66
python-dotenv>=1.0
boto3>=1.34
xmltodict>=0.13
xgboost>=2.0
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
tqdm>=4.66
requests>=2.31
